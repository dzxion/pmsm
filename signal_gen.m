clc
clear

% 定义时间参数
Fs = 100000;            % 采样频率（需满足 Nyquist 定理，建议远高于信号频率）
t = 0:1/Fs:0.1;         % 时间向量（0 到 0.1 秒，包含 10 个周期）

% 生成周期方波信号（默认占空比 50%）
f = 100;                % 信号频率 = 1/周期 = 1/0.01 = 100 Hz
signal = 30 * square(2 * pi * f * t);  % 幅值调整为 ±30